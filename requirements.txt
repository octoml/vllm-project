ninja  # For faster builds.
psutil
ray @ https://s3-us-west-2.amazonaws.com/ray-wheels/latest/ray-3.0.0.dev0-cp311-cp311-manylinux2014_x86_64.whl  # Ray versions before 2.9 conflict with pydantic >= 2.0
pandas  # Required for Ray data.
pyarrow  # Required for Ray data.
sentencepiece  # Required for LLaMA tokenizer.
numpy
einops  # Required for phi-1_5
torch >= 2.1.0
transformers >= 4.34.0  # Required for Mistral.
xformers >= 0.0.22.post7  # Required for CUDA 12.1.
fastapi
uvicorn[standard]
pydantic >= 2.5.2 # required by ollm
aioprometheus[starlette]
